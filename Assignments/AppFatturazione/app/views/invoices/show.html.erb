<%
  @hours_to_bill = Hour.where(invoice_id: @invoice.id).to_a
  @client = Client.find_by(id: @hours_to_bill.first.client_id)
  @user = @current_user
  @new_invoice = @invoice
%>

<%= render 'invoices/invoice_pdf' %>
<%=link_to 'Create PDF document', invoice_path(@invoice, :format => :pdf) %>
<%= link_to 'Back', invoices_path %>
